@model IEnumerable<MvcApplication2.DEPARTMENT>

@{
    ViewBag.Title = "Index";
}

<h2>Department</h2>

<p>
    <a href="@Url.Action("Create", "Department")" class="btn btn-primary" role="button">Add Department</a>
</p>
<br />
<br />
<br />

<div class="row">
    <div class="col-lg-12">
        <div class="ibox float-e-margins">
            <div class="dataTables_wrapper">
                <div class="ibox-content">
                    <table id="department" class="table table-striped table-bordered table-hover " style="text-align:center; width:100%">
                        <tr>
                            <th>
                                @Html.DisplayNameFor(model => model.Name)
                            </th>
                            <th></th>
                        </tr>

                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Name)
                                </td>
                                <td>
                                    @*@Html.ActionLink("Edit", "Edit", new { id = item.DepartmentId }) |
                                    @Html.ActionLink("Details", "Details", new { id = item.DepartmentId }) |
                                    @Html.ActionLink("Delete", "Delete", new { id = item.DepartmentId })*@
                                    <a href="@Url.Action("Edit", new { id = item.DepartmentId }) " style=" position:relative;" class=" btn btn-primary btn-xs">Edit</a>
                                    <a href="@Url.Action("Details", new { id = item.DepartmentId }) " style=" position:relative;" class=" btn btn-warning btn-xs">Details</a>
                                    <a href="@Url.Action("Delete", new { id = item.DepartmentId }) " style=" position:relative;" class=" btn btn-danger btn-xs">Delete</a>

                                </td>
                            </tr>
                        }

                    </table>
                </div>
            </div>
        </div>
    </div>
</div>

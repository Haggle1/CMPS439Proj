@model IEnumerable<MvcApplication2.CUSTOMER>

@{
    ViewBag.Title = "Index";
}

<h2>Customers</h2>

<p>
    <a href="@Url.Action("Create", "Customer")" class="btn btn-primary" role="button">Add Customer</a>
</p>
<br />
<br />
<br />

<div class="row">
    <div class="col-lg-12">
        <div class="ibox float-e-margins">
            <div class="dataTables_wrapper">
                <div class="ibox-content">
                    <table id="customer" class="table table-striped table-bordered table-hover " style="text-align:center; width:100%">
                        <tr>
                            <th>
                                @Html.DisplayNameFor(model => model.FName)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.LName)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.AccountBalance)
                            </th>
                            <th></th>
                        </tr>

                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.FName)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.LName)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.AccountBalance)
                                </td>
                                <td>
                                    @*@Html.ActionLink("Edit", "Edit", new { id = item.CustomerId }) |
                                    @Html.ActionLink("Details", "Details", new { id = item.CustomerId }) |
                                    @Html.ActionLink("Delete", "Delete", new { id = item.CustomerId })*@
                                    <a href="@Url.Action("Edit", new { id = item.CustomerId }) " style=" position:relative;" class=" btn btn-primary btn-xs">Edit</a>
                                    <a href="@Url.Action("Details", new { id = item.CustomerId }) " style=" position:relative;" class=" btn btn-warning btn-xs">Details</a>
                                    <a href="@Url.Action("Delete", new { id = item.CustomerId }) " style=" position:relative;" class=" btn btn-danger btn-xs">Delete</a>

                                </td>
                            </tr>
                        }

                    </table>
                </div>
            </div>
        </div>
    </div>
</div>

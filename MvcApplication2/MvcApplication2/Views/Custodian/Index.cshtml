@model IEnumerable<MvcApplication2.CUSTODIAN>

@{
    ViewBag.Title = "Index";
}

<h2>Custodians</h2>

<p>
    <a href="@Url.Action("Create", "Custodian")" class="btn btn-primary" role="button">Add Custodian</a>
</p>
<br />
<br />
<br />

<div class="row">
    <div class="col-lg-12">
        <div class="ibox float-e-margins">
            <div class="dataTables_wrapper">
                <div class="ibox-content">
                    <table id="custodian" class="table table-striped table-bordered table-hover " style="text-align:center; width:100%">
                        <tr>
                            <th>
                                @Html.DisplayNameFor(model => model.FName)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.LName)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.DateOfEmployment)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.HourlyPay)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.NumberOfYearsEmployed)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.FARMERS_MARKET.Address)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.PROPIETOR.FName)
                            </th>
                            <th></th>
                        </tr>

                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.FName)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.LName)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.DateOfEmployment)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.HourlyPay)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.NumberOfYearsEmployed)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.FARMERS_MARKET.Address)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.PROPIETOR.FName)
                                </td>
                                <td>
                                    @*@Html.ActionLink("Edit", "Edit", new { id = item.CustodianId }) |
                                    @Html.ActionLink("Details", "Details", new { id = item.CustodianId }) |
                                    @Html.ActionLink("Delete", "Delete", new { id = item.CustodianId })*@
                                    <a href="@Url.Action("Edit", new { id = item.CustodianId }) " style=" position:relative;" class=" btn btn-primary btn-xs">Edit</a>
                                    <a href="@Url.Action("Details", new { id = item.CustodianId }) " style=" position:relative;" class=" btn btn-warning btn-xs">Details</a>
                                    <a href="@Url.Action("Delete", new { id = item.CustodianId }) " style=" position:relative;" class=" btn btn-danger btn-xs">Delete</a>

                                </td>
                            </tr>
                        }

                    </table>
                </div>
            </div>
        </div>
    </div>
</div>

